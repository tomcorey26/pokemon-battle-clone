const sum = require("./app");

test("adds 1 + 2 to equal 3", () => {
  expect(sum(1, 2)).toBe(3);
});

//when player has no pokemon left game is over

//when enemy has no pokemon left game is over

//when user chooses to run game is over
